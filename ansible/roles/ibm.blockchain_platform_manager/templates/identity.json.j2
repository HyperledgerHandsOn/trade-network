{# SPDX-License-Identifier: Apache-2.0 #}
{
    "name": "{{ identity }}",
    "msp_id": "{{ msp_id }}",
    "cert": "{{ certificate | b64encode }}",
    "private_key": "{{ private_key | b64encode }}",
}